---
alwaysApply: true
---

# QA Conventions for TDD

## Общие принципы

- **TDD цикл**: Red → Green → Refactor. Тест пишется ДО реализации
- **Только критичный функционал**: Не тестировать геттеры, сеттеры, тривиальный код
- **KISS**: Один тест = одна проверка. Простые, понятные тесты
- **DRY**: Использовать fixtures для повторяющейся логики, не копипастить setup код

## Что тестировать

✅ **ДА**:
- Бизнес-логика и алгоритмы
- Обработка граничных случаев (пустые данные, лимиты, ошибки)
- Изоляция данных между сущностями (юзеры, сессии)
- Интеграционные точки (API, БД) - моки обязательны
- Критичные state changes

❌ **НЕТ**:
- Тривиальные геттеры/сеттеры
- Код библиотек (pytest, aiogram и т.д.)
- Очевидные вещи без логики
- 100% покрытие ради покрытия

## Структура тестов

```python
def test_feature_scenario() -> None:
    """Описание: что проверяем и почему важно."""
    # Arrange: минимальная подготовка данных
    obj = MyClass(key_param=value)
    
    # Act: одно действие
    result = obj.do_something(input_data)
    
    # Assert: проверка ожидаемого результата
    assert result == expected
```

## Naming

- `test_<feature>_<scenario>`: `test_history_limit`, `test_clear_nonexistent_user`
- Файлы: `test_<module>.py` соответствует `src/<module>.py`
- Краткие docstrings на русском: что проверяется, зачем

## Fixtures (conftest.py)

- Общие setup/teardown для модуля
- Моки внешних зависимостей (LLM API, Telegram API)
- Тестовые данные, используемые в 3+ тестах
- **Не создавать** фикстуры для одноразовых данных

## Async тесты

```python
async def test_async_feature() -> None:
    """Описание async теста."""
    result = await async_function()
    assert result is not None
```

Pytest-asyncio настроен в `pyproject.toml`, не нужны декораторы.

## Моки

- Мокировать только внешние зависимости: API, БД, файлы, сеть
- Не мокировать свой код - тестировать реальное взаимодействие
- `unittest.mock.patch` для monkey-patching внешних вызовов

## Параметризация

Использовать для проверки множественных входных данных:

```python
@pytest.mark.parametrize("input,expected", [
    ([], 0),
    ([1], 1),
    ([1, 2, 3], 6),
])
def test_sum_cases(input: list[int], expected: int) -> None:
    assert sum(input) == expected
```

## Type hints

- Обязательны для параметров и возвращаемых значений
- `-> None` для тестов без возврата
- Соответствие mypy strict mode из `pyproject.toml`

## Assertions

- Используй стандартный `assert`, не `self.assertEqual`
- Одна проверка на тест (исключения: связанные проверки одного результата)
- Явные сравнения: `assert len(items) == 0`, не `assert not items`

## Исключения

```python
def test_invalid_input_raises() -> None:
    """Проверка что некорректный ввод вызывает исключение."""
    with pytest.raises(ValueError, match="Invalid"):
        process_invalid_data()
```

## Чего избегать

- ❌ Тестов без assert
- ❌ Тестов "для покрытия" без реальной проверки
- ❌ Дублирования логики из production кода в тесте
- ❌ Множественных assert для несвязанных проверок - разбить на тесты
- ❌ Hardcoded значений - использовать константы/fixtures
- ❌ Тестов, зависящих от порядка выполнения

## Организация

```
tests/
├── conftest.py           # Общие fixtures
├── test_conversation.py  # Тесты для src/llm/conversation.py
├── test_settings.py      # Тесты для src/config/settings.py
└── test_bot.py          # Тесты для src/bot/*
```

## Запуск

- `pytest` - все тесты
- `pytest tests/test_specific.py` - один файл
- `pytest -k test_name` - конкретный тест
- `pytest -v` - verbose output
- `pytest --cov=src` - покрытие (не цель, но полезный индикатор)

## Red flags при ревью

🚩 Тест проверяет trivial код без логики
🚩 Тест > 20 строк (возможно, слишком сложный)
🚩 Тест использует sleep/time.time() (не детерминированный)
🚩 Тест модифицирует глобальное состояние без cleanup
🚩 Нет docstring с описанием проверки

## TDD Workflow

1. **Red**: Написать failing тест для новой фичи
2. **Green**: Реализовать минимальный код для прохождения теста
3. **Refactor**: Улучшить код без изменения функциональности
4. Коммитить только когда все тесты зеленые
5. Не пропускать refactor фазу
# QA Conventions for TDD

## Общие принципы

- **TDD цикл**: Red → Green → Refactor. Тест пишется ДО реализации
- **Только критичный функционал**: Не тестировать геттеры, сеттеры, тривиальный код
- **KISS**: Один тест = одна проверка. Простые, понятные тесты
- **DRY**: Использовать fixtures для повторяющейся логики, не копипастить setup код

## Что тестировать

✅ **ДА**:
- Бизнес-логика и алгоритмы
- Обработка граничных случаев (пустые данные, лимиты, ошибки)
- Изоляция данных между сущностями (юзеры, сессии)
- Интеграционные точки (API, БД) - моки обязательны
- Критичные state changes

❌ **НЕТ**:
- Тривиальные геттеры/сеттеры
- Код библиотек (pytest, aiogram и т.д.)
- Очевидные вещи без логики
- 100% покрытие ради покрытия

## Структура тестов

```python
def test_feature_scenario() -> None:
    """Описание: что проверяем и почему важно."""
    # Arrange: минимальная подготовка данных
    obj = MyClass(key_param=value)
    
    # Act: одно действие
    result = obj.do_something(input_data)
    
    # Assert: проверка ожидаемого результата
    assert result == expected
```

## Naming

- `test_<feature>_<scenario>`: `test_history_limit`, `test_clear_nonexistent_user`
- Файлы: `test_<module>.py` соответствует `src/<module>.py`
- Краткие docstrings на русском: что проверяется, зачем

## Fixtures (conftest.py)

- Общие setup/teardown для модуля
- Моки внешних зависимостей (LLM API, Telegram API)
- Тестовые данные, используемые в 3+ тестах
- **Не создавать** фикстуры для одноразовых данных

## Async тесты

```python
async def test_async_feature() -> None:
    """Описание async теста."""
    result = await async_function()
    assert result is not None
```

Pytest-asyncio настроен в `pyproject.toml`, не нужны декораторы.

## Моки

- Мокировать только внешние зависимости: API, БД, файлы, сеть
- Не мокировать свой код - тестировать реальное взаимодействие
- `unittest.mock.patch` для monkey-patching внешних вызовов

## Параметризация

Использовать для проверки множественных входных данных:

```python
@pytest.mark.parametrize("input,expected", [
    ([], 0),
    ([1], 1),
    ([1, 2, 3], 6),
])
def test_sum_cases(input: list[int], expected: int) -> None:
    assert sum(input) == expected
```

## Type hints

- Обязательны для параметров и возвращаемых значений
- `-> None` для тестов без возврата
- Соответствие mypy strict mode из `pyproject.toml`

## Assertions

- Используй стандартный `assert`, не `self.assertEqual`
- Одна проверка на тест (исключения: связанные проверки одного результата)
- Явные сравнения: `assert len(items) == 0`, не `assert not items`

## Исключения

```python
def test_invalid_input_raises() -> None:
    """Проверка что некорректный ввод вызывает исключение."""
    with pytest.raises(ValueError, match="Invalid"):
        process_invalid_data()
```

## Чего избегать

- ❌ Тестов без assert
- ❌ Тестов "для покрытия" без реальной проверки
- ❌ Дублирования логики из production кода в тесте
- ❌ Множественных assert для несвязанных проверок - разбить на тесты
- ❌ Hardcoded значений - использовать константы/fixtures
- ❌ Тестов, зависящих от порядка выполнения

## Организация

```
tests/
├── conftest.py           # Общие fixtures
├── test_conversation.py  # Тесты для src/llm/conversation.py
├── test_settings.py      # Тесты для src/config/settings.py
└── test_bot.py          # Тесты для src/bot/*
```

## Запуск

- `pytest` - все тесты
- `pytest tests/test_specific.py` - один файл
- `pytest -k test_name` - конкретный тест
- `pytest -v` - verbose output
- `pytest --cov=src` - покрытие (не цель, но полезный индикатор)

## Red flags при ревью

🚩 Тест проверяет trivial код без логики
🚩 Тест > 20 строк (возможно, слишком сложный)
🚩 Тест использует sleep/time.time() (не детерминированный)
🚩 Тест модифицирует глобальное состояние без cleanup
🚩 Нет docstring с описанием проверки

## TDD Workflow

1. **Red**: Написать failing тест для новой фичи
2. **Green**: Реализовать минимальный код для прохождения теста
3. **Refactor**: Улучшить код без изменения функциональности
4. Коммитить только когда все тесты зеленые
5. Не пропускать refactor фазу
