---
alwaysApply: true
---
## 📋 Цикл работы над итерацией с TDD подходом (8 шагов)

### 1. Планирование
- Прочитать текущую итерацию из `../../docs/tasklist.md`
- Изучить требования и цель итерации
- Проверить список задач

### 2. Согласование решения
**Перед началом реализации:**
- Предложить план реализации с конкретными действиями
- Показать **отрезки кода** или структуру файлов
- Указать, какие файлы будут созданы/изменены
- Объяснить архитектурные решения (включая выделение приватных методов для читаемости)
- **Описать план тестирования** - какие тесты будут написаны
- **Дождаться согласия пользователя**

### 3. Реализация по TDD
**После получения согласия:**

Следовать циклу **Red-Green-Refactor** для каждой задачи:

#### 🔴 Фаза RED (Красная полоса)
1. **Написать failing тест:**
   - Создать/обновить тестовый файл в `tests/`
   - Написать тест, который описывает желаемое поведение
   - Тест должен провалиться (функционал еще не реализован)
   
2. **Запустить тест:**
   - Выполнить `make test` или `pytest tests/test_<module>.py`
   - **Убедиться, что тест падает** с ожидаемой ошибкой
   - Показать результат пользователю

3. **Подтверждение RED:**
   - Объяснить, почему тест падает
   - **Дождаться подтверждения перехода к GREEN фазе**

#### 🟢 Фаза GREEN (Зеленая полоса)
1. **Написать минимальный код:**
   - Реализовать функционал, чтобы тест прошел
   - Следовать принципам из `../../docs/vision.md`:
     - 1 класс = 1 файл
     - Строгая типизация (type hints с современным синтаксисом PEP 585: `list[str]`, `dict[str, int]`, `| None`)
     - Обязательные зависимости без Optional (если параметр обязателен, не использовать Optional)
     - Docstrings для классов и методов
     - Читаемость кода превыше всего
   - **Цель: сделать тест зеленым, не больше**

2. **Запустить тест:**
   - Выполнить `make test`
   - **Убедиться, что тест проходит**
   - Показать результат пользователю

3. **Подтверждение GREEN:**
   - Объяснить, как код удовлетворяет требованиям теста
   - **Дождаться подтверждения перехода к REFACTOR фазе**

#### ♻️ Фаза REFACTOR (Рефакторинг)
1. **Улучшить код (если необходимо):**
   - Удалить дублирование
   - Улучшить читаемость
   - Оптимизировать структуру
   - Выделить приватные методы для лучшей организации
   - **Сохранить работоспособность всех тестов**

2. **Запустить все тесты:**
   - Выполнить `make test`
   - **Убедиться, что все тесты проходят**
   - Показать результат пользователю

3. **Подтверждение REFACTOR:**
   - Объяснить, какие улучшения были внесены
   - **Дождаться подтверждения завершения цикла**

**Повторить цикл RED-GREEN-REFACTOR для каждой функции/метода/требования**

### 4. Финальное тестирование
**После завершения всех TDD циклов:**
- Запустить `make format` - автоматическое форматирование кода
- Запустить `make lint` - проверка линтером без ошибок
- Запустить `make typecheck` - статическая проверка типов через mypy
- Запустить `make test` - **все unit-тесты должны проходить**
- Запустить приложение через `make run` (или соответствующую команду)
- Проверить работоспособность приложения
- Выполнить проверку из раздела **"Проверка"** в ../../docs/tasklist.md для текущей итерации
- Убедиться, что приложение способно выполнить реализованную задачу
- Исправить ошибки, если они обнаружены
- **Показать результаты тестирования пользователю**

**Комплексная проверка:**
- Запустить `make quality` - все проверки сразу (format + lint + typecheck + test)

### 5. Подтверждение
**После успешного тестирования:**
- Показать, что было реализовано
- Указать созданные/измененные файлы (как тесты, так и код)
- Показать покрытие тестами
- Показать результаты проверки из ../../docs/tasklist.md
- **Дождаться подтверждения от пользователя**

### 6. Обновление прогресса
**После получения подтверждения:**
- Обновить таблицу прогресса в `../../docs/tasklist.md`:
  - Изменить статус на ✅ Завершено
  - Указать дату завершения
- Отметить галочками выполненные задачи (заменить `[ ]` на `[x]`)

### 7. Коммит
**После обновления tasklist:**
- Сделать коммит с понятным сообщением:
  - Формат: `feat: Iteration N - [краткое описание]`
  - Пример: `feat: Iteration 1 - Project setup and configuration`
- Включить в коммит все изменения итерации (тесты и код)
- **НЕ** делать force push на main/master

### 8. Согласование перехода
**Перед началом следующей итерации:**
- Спросить о готовности к следующей итерации
- Дождаться явного подтверждения
- Только после подтверждения переходить к шагу 1

---

## 🔄 TDD Цикл: Red-Green-Refactor

```
┌─────────────────────────────────────────┐
│  1. RED: Написать failing тест          │
│     ↓ make test → FAILED ✗              │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  2. GREEN: Написать код для прохода теста│
│     ↓ make test → PASSED ✓              │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  3. REFACTOR: Улучшить код              │
│     ↓ make test → PASSED ✓              │
└─────────────────────────────────────────┘
              ↓
        [Следующая функция]
```

---

## ✅ Правила отметки задач

**Формат:**
- Невыполненная: `- [ ] Задача`
- Выполненная: `- [x] Задача`

**В таблице прогресса:**
- ⏳ Ожидает - итерация не начата
- 🚧 В работе - итерация выполняется
- ✅ Завершено - итерация полностью завершена и протестирована
- ⚠️ Проблемы - есть проблемы

---

## 🚫 Чего НЕ делать

- ❌ НЕ переходить к следующей итерации без завершения текущей
- ❌ НЕ реализовывать код без предварительного согласования
- ❌ НЕ писать код перед тестами (нарушение TDD)
- ❌ НЕ переходить к GREEN фазе с проходящим тестом
- ❌ НЕ переходить к REFACTOR фазе с падающим тестом
- ❌ НЕ делать коммит без явного запроса/согласия
- ❌ НЕ добавлять функционал, не описанный в ../../docs/tasklist.md
- ❌ НЕ усложнять решение - следовать KISS
- ❌ НЕ нарушать правило "1 класс = 1 файл"
- ❌ НЕ делать force push или destructive git операции
- ❌ НЕ пропускать фазы TDD цикла

---

## 📝 Шаблон согласования

### Перед реализацией (Планирование):

```
## Итерация N: [Название]

### Планируемые изменения:

#### Тесты:
1. **Создать/обновить `tests/test_<module>.py`**
   ```python
   def test_feature_name():
       """Тест для проверки [описание]."""
       # Arrange
       setup = ...
       
       # Act
       result = ...
       
       # Assert
       assert result == expected
   ```

#### Код:
1. **Создать файл `path/to/file.py`**
   ```python
   class ClassName:
       """Краткое описание."""
       
       def method_name(self) -> ReturnType:
           # Основная логика
   ```

2. **Изменить файл `path/to/existing.py`**
   - Добавить метод X
   - Изменить логику Y

### Архитектурные решения:
- [Объяснение ключевых решений]

### План TDD циклов:
1. Цикл 1: Тест для [функция A] → Реализация A → Рефакторинг
2. Цикл 2: Тест для [функция B] → Реализация B → Рефакторинг

Согласовать?
```

### После RED фазы:

```
🔴 RED: Тест написан и падает

**Файл:** tests/test_<module>.py
**Результат:**
```
FAILED tests/test_<module>.py::test_feature_name
AssertionError: [ожидаемая ошибка]
```

**Объяснение:** Тест падает, потому что [причина].

Переходим к GREEN фазе?
```

### После GREEN фазы:

```
🟢 GREEN: Тест проходит

**Файл:** src/path/to/file.py
**Результат:**
```
PASSED tests/test_<module>.py::test_feature_name
```

**Объяснение:** Реализован минимальный код для прохождения теста.

Переходим к REFACTOR фазе?
```

### После REFACTOR фазы:

```
♻️ REFACTOR: Код улучшен

**Изменения:**
- [Список улучшений]

**Результат:**
```
PASSED tests/test_<module>.py (все тесты)
```

Завершаем цикл? Переходим к следующей функции?
```

---

## 🔄 Пример полного TDD цикла

1. **Планирование**: "Начинаем Итерацию 1: Настройка проекта с TDD"
2. **Согласование**: "Планирую создать структуру директорий, файлов и тестов: [показать структуру, ключевые отрезки кода и тестов]. Согласовать?"
3. **[Ожидание]**: Пользователь → "Да, реализуй"
4. **TDD Цикл 1 - RED**: "Написал тест test_config_loading. Запускаю `make test`. Результат: FAILED [показать ошибку]. Переходим к GREEN?"
5. **[Ожидание]**: Пользователь → "Да"
6. **TDD Цикл 1 - GREEN**: "Реализовал минимальный код для загрузки конфига. Запускаю `make test`. Результат: PASSED. Переходим к REFACTOR?"
7. **[Ожидание]**: Пользователь → "Да"
8. **TDD Цикл 1 - REFACTOR**: "Улучшил структуру кода, выделил приватный метод _validate_config. Запускаю `make test`. Результат: PASSED. Следующая функция?"
9. **[Повторение циклов для остальных функций]**
10. **Финальное тестирование**: "Запускаю `make quality`. Результат: [показать результаты]"
11. **Подтверждение**: "Реализовал Итерацию 1. Создано: [список файлов с тестами и кодом]. Покрытие: [процент]. Проверка пройдена: [результаты]. Подтверждаешь завершение?"
12. **[Ожидание]**: Пользователь → "Подтверждаю"
13. **Обновление**: [Обновление ../../docs/tasklist.md - статус, даты, галочки]
14. **Коммит**: `git commit -m "feat: Iteration 1 - Project setup and configuration"`
15. **Переход**: "Итерация 1 завершена. Переходим к Итерации 2?"
16. **[Ожидание]**: Пользователь → "Да" → возврат к шагу 1

---

## 📖 Справочные документы

Всегда держать в контексте:
- **../../docs/tasklist.md** - план разработки и текущий прогресс
- **../../docs/vision.md** - архитектура, принципы, технологии
- **conventions.mdc** - стандарты кодирования
- **qa_conventions.mdc** - стандарты тестирования

---

## 💡 Принципы TDD

1. **Тест сначала**: Всегда пишем тест до кода
2. **Минимальная реализация**: В GREEN фазе пишем только то, что нужно для прохождения теста
3. **Рефакторинг под защитой тестов**: Улучшаем код, зная что тесты его проверяют
4. **Маленькие шаги**: Каждый цикл RED-GREEN-REFACTOR - это один маленький шаг
5. **Быстрая обратная связь**: Часто запускаем тесты
6. **Документация через тесты**: Тесты показывают, как использовать код

---

**Главное**: Согласование → [RED → GREEN → REFACTOR] × N → Финальное тестирование → Подтверждение → Обновление → Коммит → Переход
