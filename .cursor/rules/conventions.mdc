---
alwaysApply: false
---
## Правила кодирования

### Обязательно
- ✅ **Type hints** - строгая типизация всех параметров и возвращаемых значений
- ✅ **Современный синтаксис типов** - использовать `list[str]` вместо `List[str]` (PEP 585)
- ✅ **Docstrings** - для всех классов и публичных методов
- ✅ **Явное > неявного** - никаких неочевидных магических решений
- ✅ **Минимум абстракций** - прямолинейный код
- ✅ **Обязательные зависимости без Optional** - если параметр обязателен, не использовать Optional

### Запрещено
- ❌ Сложные паттерны проектирования
- ❌ Излишняя абстракция и многослойность
- ❌ Преждевременная оптимизация
- ❌ Умный код в ущерб читаемости
- ❌ Магические числа - использовать константы

---

## Структура кода

### Организация
```
src/
├── bot/           # TelegramBot, MessageHandler
├── llm/           # LLMClient, Conversation
├── config/        # Settings
└── main.py        # Точка входа
```

### Импорты
- Стандартная библиотека → внешние пакеты → локальные модули
- Абсолютные импорты от корня `src/`

### Naming
- Классы: `PascalCase`
- Функции/методы: `snake_case`
- Константы: `UPPER_SNAKE_CASE`
- Приватные: префикс `_`

---

## Технологии

### Управление проектом (обязательно)
- **uv** - управление зависимостями и виртуальным окружением (pyproject.toml)
- **make** - автоматизация задач (setup, run, clean)

### Используем
- Python 3.x (системная версия)
- aiogram 3.x (polling)
- openai (для OpenRouter)
- pydantic (валидация)
- python-dotenv (конфигурация)
- logging (стандартный)

### НЕ используем
- ❌ Базы данных (только in-memory)
- ❌ Redis/кэширование
- ❌ Docker (пока)
- ❌ Сложные фреймворки
- ❌ Системы мониторинга

---

## Обработка ошибок

### Подход
- Логирование с правильным уровнем (INFO/WARNING/ERROR)
- Понятные сообщения пользователю
- Никаких try-except "на всякий случай"

### Логирование
- INFO: старт/стоп, сообщения пользователей, успешные операции
- WARNING: достижение лимитов, медленные операции
- ERROR: API ошибки, таймауты, сбои конфигурации
- ❌ НЕ логируем токены и API ключи

---

## Конфигурация

### .env файл
- Обязательные: `TELEGRAM_BOT_TOKEN`, `OPENROUTER_API_KEY`
- Опциональные: с дефолтными значениями
- Валидация через Pydantic Settings
- Fail-fast при отсутствии обязательных

### Системный промпт
- Hardcode в коде (не в .env)
- Простой и понятный текст

---

## Что НЕ делать

- ❌ Сложная архитектура
- ❌ Абстрактные фабрики и билдеры
- ❌ Middleware и декораторы "на всякий случай"
- ❌ Персистентность данных (in-memory для MVP)
- ❌ Преждевременное масштабирование

---

## 🎨 Форматирование кода

### Инструмент: Ruff
- **Максимальная длина строки**: 100 символов
- **Версия Python**: 3.10+
- **Автоматическое форматирование**: `make format`
- **Сортировка импортов**: автоматически через ruff

### Правила форматирования
- Группировка импортов: stdlib → external → local
- Два пустых строки между top-level определениями
- Одна пустая строка между методами класса
- Trailing comma в многострочных структурах
- Кавычки: двойные для строк, одинарные для однобуквенных

### Команды
```bash
make format  # Отформатировать код
make lint    # Проверить код линтером
```

---

## 🔍 Линтинг и статический анализ

### Ruff (линтер + форматтер)
**Включенные правила**:
- `E` - pycodestyle errors
- `F` - pyflakes
- `I` - isort (сортировка импортов)
- `N` - pep8-naming
- `W` - pycodestyle warnings
- `B` - flake8-bugbear (поиск багов)
- `SIM` - flake8-simplify (упрощение кода)
- `UP` - pyupgrade (современный синтаксис)

**Команда**: `make lint`

### Mypy (проверка типов)
**Режим**: strict
- Проверка всех type hints
- Предупреждения о `Any`
- Проверка неиспользуемых конфигов

**Команда**: `make typecheck`

### Обязательно
- ✅ Код проходит `make lint` без ошибок
- ✅ Код проходит `make typecheck` без ошибок (после настройки mypy)
- ✅ Все предупреждения исправлены

---

## 🧪 Стандарты тестирования

### Инструменты
- **pytest** - фреймворк для тестирования
- **pytest-asyncio** - поддержка async/await тестов
- **pytest-cov** - измерение покрытия кода

### Структура тестов
```
tests/
├── __init__.py
├── conftest.py          # Fixtures и настройки
├── test_conversation.py # Тесты для Conversation
├── test_settings.py     # Тесты для Settings
└── ...
```

### Правила тестирования
- **Naming**: `test_<что_тестируем>_<ожидаемое_поведение>`
- **Arrange-Act-Assert**: структурировать тесты в 3 блока
- **Один тест = одна проверка**: не тестировать всё в одном тесте
- **Изоляция**: тесты не должны зависеть друг от друга
- **Fixtures**: использовать для переиспользуемой setup логики

### Coverage таргеты
- **Критичная бизнес-логика**: 80%+ (Conversation, Settings)
- **Вспомогательные модули**: 60%+
- **Интеграции с API**: моки для внешних сервисов

### Команды
```bash
make test                          # Запустить тесты
pytest --cov=src --cov-report=term-missing  # С покрытием
```

---

## 🐍 Лучшие практики Python

### Типизация
```python
# ✅ Хорошо (PEP 585, Python 3.10+)
def process(items: list[str]) -> dict[str, int]:
    return {item: len(item) for item in items}

# ❌ Плохо (устаревший синтаксис)
from typing import List, Dict
def process(items: List[str]) -> Dict[str, int]:
    return {item: len(item) for item in items}

# ✅ Хорошо (обязательный параметр)
def __init__(self, client: LLMClient) -> None:
    self.client = client

# ❌ Плохо (ненужный Optional)
def __init__(self, client: Optional[LLMClient] = None) -> None:
    self.client = client
```

### Константы вместо магических чисел
```python
# ✅ Хорошо
MAX_MESSAGE_LENGTH = 4000
if len(text) > MAX_MESSAGE_LENGTH:
    split_message(text)

# ❌ Плохо
if len(text) > 4000:
    split_message(text)
```

### Выделение методов
```python
# ✅ Хорошо - метод короткий и понятный
async def handle_text(self, message: Message) -> None:
    response = await self.llm_client.get_response(message.text)
    parts = self._split_message(response)
    await self._send_parts(message, parts)

def _split_message(self, text: str) -> list[str]:
    """Разбить длинное сообщение на части."""
    # Логика разбивки

# ❌ Плохо - метод слишком длинный
async def handle_text(self, message: Message) -> None:
    response = await self.llm_client.get_response(message.text)
    # 50 строк логики разбивки сообщений
    # 20 строк логики отправки
```

### Docstrings
```python
# ✅ Хорошо
def add_message(self, user_id: int, role: str, content: str) -> None:
    """
    Добавить сообщение в историю пользователя.
    
    Args:
        user_id: ID пользователя Telegram
        role: Роль отправителя ("user" или "assistant")
        content: Содержимое сообщения
    """
    pass

# ❌ Плохо (нет docstring для публичного метода)
def add_message(self, user_id: int, role: str, content: str) -> None:
    pass
```

### Обработка ошибок
```python
# ✅ Хорошо - конкретные исключения
except APITimeoutError as e:
    logger.error(f"Timeout: {e}")
    raise TimeoutError("Превышено время ожидания") from e

# ❌ Плохо - широкое перехватывание
except Exception:
    pass
```

---

## Чеклист при генерации кода

1. ✅ Type hints на всех параметрах и возвратах (PEP 585)
2. ✅ Docstring для класса и публичных методов
3. ✅ 1 класс = 1 файл
4. ✅ Простое и понятное решение
5. ✅ Логирование важных операций
6. ✅ Обработка ошибок с понятными сообщениями
7. ✅ Код читается как книга
8. ✅ Код отформатирован (ruff format)
9. ✅ Код проходит линтер (ruff check)
10. ✅ Нет магических чисел (использовать константы)
11. ❌ Нет лишних абстракций
12. ❌ Нет сложных паттернов
13. ❌ Нет оверинжиниринга

---

## 🔄 Рабочий процесс

### Перед коммитом
```bash
make format     # Форматировать код
make lint       # Проверить линтером
make typecheck  # Проверить типы (если mypy настроен)
make test       # Запустить тесты (если есть)
make quality    # Все проверки сразу
```

### После внесения изменений
1. Отформатировать: `make format`
2. Проверить качество: `make quality`
3. Запустить приложение: `make run`
4. Убедиться, что всё работает
5. Закоммитить изменения

---

**Принцип**: Код должен быть настолько прост, что его сможет понять и поддержать любой разработчик без документации. Автоматизированные проверки помогают поддерживать качество.
